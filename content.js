// Lắng nghe thông điệp từ background script
/*chrome.runtime.onMessage.addListener(function (message, sender, sendResponse) {
    if (message.clipboardData) {
      var clipboardData = message.clipboardData;
      console.log("Clipboard Data in popup:", clipboardData);
      // Xử lý dữ liệu từ clipboard ở đây
    }
});*/